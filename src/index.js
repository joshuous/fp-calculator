'use strict';

function calculate(
  newAction = 'ac',
  prevAction = 'ac',
  currDisplay = '0',
  currExpression = ''
) {
  return {
    display: undefined,
    expression: undefined,
  };
}

module.exports = calculate;
